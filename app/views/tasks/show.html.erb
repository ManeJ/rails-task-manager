<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<p><%= check_box("task", @task.id, {checked: @task.completed}) %></p>
<h3><%= link_to 'Edit task', edit_task_path %></h3>
<h3><%= link_to 'Back', tasks_path(@tasks) %></h3
